# CHANGELOG — ps-mcp

版本语义：patch=修错不改行为（含既有强制格式的字段增补/标注微调）；minor=增量条款/工具；
major=行为契约变更（**必须立版说明**）。④b 断言机器核对"CHANGELOG 最新条目版本 ==
SKILL frontmatter 版本"。

## 0.7.1 (2026-09-19)

- **全景报告吸收**（docs/panorama-absorption-20260919.md）：blender 50 轮逐项对照
  三档判定——立即落地 5 项（Px"修正指令必须引用所见"=新指纹 #26；铁律 8 补 △ 出口；
  开工消息加"视觉复述"行；HANDOFF 四项自检+governance 冻结线；worked example 成文
  =docs/worked-example-grid-removal.md）；登记触发条件 4 项（部署多根/视觉义务穷举/
  fragments 命中率/docs 引用编号化——不预建）。
- **CL-X-08 晋升清单项**（工艺库清单项层首条，实战 2 次达线）。
- **grid 二轮精修完成**：残差 α 二次校正（v1485 单列 α=0.039 逆除），Px 实看通过，
  **v3 为主档**（AGENT_grid_removed_v3.png/.jpg）；残余 p25 信号经视觉裁定=内容
  对比度非线痕——"机查定位+视觉定罪"互补性成文于 worked example。
- 台账编辑事故教训：shell heredoc 在 GBK 环境写 UTF-8 有乱码风险（本机实测显示层
  损坏、文件层完好）——**台账/包文件追加一律走 UTF-8 工具（py）**。

## 0.7.0 (2026-09-19)

- **D13 视觉公理（F-16 闭环，用户裁决）**：多模态能力深度融合进每一层——
  **铁律 8**（视觉实看贯穿全流程，判定分辨率不足=没看）；门双通道改造（G0 视觉
  首察=视觉发现清单驱动方案假设；新增 **Px 过程检查点**=每写操作后就地 1:1
  实看-修正循环，02 分册 §7；G3 成品视觉检验=沿处理区 1:1 瓦片全长；G4 展示包）；
  分辨率纪律（质量判定必须在缺陷可见的分辨率上做出）；工艺库**核（视觉核验点）**
  字段——条目驱动操作自动调度感知（CL-A-08 首个带核条目）；宣告行附**实看清单**
  （与文件系统互证，治理可机查"是否真看过"）。触发=grid 任务 v1 G1 被拒的系统性
  实证。新指纹 #25。
- grid 任务进行中：v1 被用户 G1 否决（残痕）→ v2 横截面 α 逆混合（α=0.50）+
  1:1 瓦片全长实看通过待用户裁决；实战产出 CL-A-08 v2 方法与 CL-X-08（1:1 分块
  实看义务）候选。

## 0.6.1 (2026-09-19)

- **F-2 闭环（已代处置并验证）**：探明暂存盘偏好的 AM 路径——`scratchDiskPreferences`
  在 application 属性层，`scratchDisks` 为字符串数组（"启动"=系统盘）；`setd` 新值键
  为 `T(to)`（非属性名，两轮失败后由 Listener 结构修正）；代处置 暂存盘=[D:\, 启动]，
  退出落盘+冷启动读回持久化 ✓、启动警告消失 ✓（探测链 4 步留 docs/research §7；
  C: 22GB 本身建议日后清理）。ExtendScript ES3 无 forEach 的坑一并入册。
- **F-3 闭环（✓实测）**：`Photoshop.exe <script.jsx>` 冷启动全链路 16s（启动+执行+
  落盘+回读），中文/全角/空格路径 OK，`$.fileName` 为 URI 编码；菜单触发留用户路径
  未测；**新 F-15**：启动模态在场时 F 模式是否被阻未测（F 兜底成立性的场景缺口）。
- 01 分册 §5 / INSTALL 模式表回写 ✓实测。

## 0.6.0 (2026-09-19)

- **本地 LM Studio 集成（D12，用户问询触发的设计修正）**：候选取用改**三级递进**
  ——L1 分层（P1 常读/P2 条件触发，一次性标定）、L2 任务型组合缓存（**首次排一次写
  回固化，同型任务复用**，craft-library §3 缓存表就位）、L3 兜底重排（仅新任务型/
  条件全不命中/池跨 200 条）。craft-library 候选层完成 P1/P2 标定。
- **工具** `scripts/rerank_local.py`：经本地 LM Studio（用户自有，127.0.0.1:8080）
  驱动 qwen3-reranker-4b（chat yes/no 判定式，`/no_think`+max_tokens 200+重试；
  该版 LM Studio 无 /v1/rerank 端点——实测）；embeddings 端点与 qwen3-embedding-8b
  （4096 维）探明，未来 RAG 就绪。自噬通过（双极性判定正确）。**可选依赖**：Server
  未开时 L3 兜底=agent 内判断，不阻塞（F-14）。
- **P1/P2 全矩阵校准**（120 对，docs/craft-research/p1p2-calibration-20260919.md）：
  重排器强于条件归属（P2 桶全中），**对 meta 规则全 0（X 节）+漏检 E/G 措辞**——
  治理规则实证：P1 由治理指定、P2 由重排器建议+人工确认、L3 输出仅参考（D12）。
- 新指纹 #24（L2 组合缓存固化）；LEDGER 编辑事故一次（F-13 被 F-14 覆盖）当场发现
  当场恢复——append-only 纪律由 runner ④a 兜底验证。

## 0.5.0 (2026-09-19)

- **用户四项裁决落地（decision-brief）→ 工艺库建库**（F-11 闭环）：决策一选**选项 1**
  （静态清单项库+运行时模型重排）——craft-library.md 重建为两层结构：**清单项层**
  （实战验证条目，暂空待首个真实任务积累）+ **候选层 34 条**（从 CR 池筛选，每条带
  CR-<分支>-NN 指针/来源/争议标/as-of）；重排步（rubric=匹配度>证据强度>新鲜度>
  争议只呈现）与 top-k 留痕纪律写进 00 分册；向量 RAG 按切换阈值挂待办不预建。
- **决策二 a+c**：新增 X 跨分支码（所有任务都读 X 节，候选 7 条）；X 审计结论=无
  条目够无条件铁律格，留在 X 节。**决策三 c / 决策四 c** 追认（时效数值带 as-of+
  交付现查红线；争议两派并列禁升清单项——底稿既存形态）。
- **00 分册新增混合用途检测**（用户指令）：主分支判定后主动扫多分支信号（多工艺
  动词/多形态产物/异质文件集），检出→开工消息列【主分支】+【次分支】、门配置取并集、
  占比 ≥40% 或独立交付物升并列主分支。
- **指纹 #19 放宽**（裁决）：官方级来源可候选态登记——措辞改"清单项必须实战出处"；
  新指纹 #22（重排 top-k 留痕）/ #23（混合检测声明）。SKILL §3 指针同步。

## 0.4.2 (2026-09-19)

- **F-11 范畴·多通道深读重试成功三个目标**（curl 独立网络路径破局；通道轮换教训入
  overview §4）：fstoppers FS Rehab 原文（CR-A-04a 补强：FS=末位问题定性工具/若用
  放前端/低频层无限编辑假设错误）、Sprout Social 平台规格全表（2026-05-11 版，
  LinkedIn/FB 封面等多处数值比搜索摘要新——CR-C-11 更新并留痕过时值作时效实证）、
  ConradChavez P3 原文（CR-X-03 升一手证据）。**web.dev 三通道全灭判"本网络不可达"**，
  留待补。
- **F-11 范畴·裁决说明书**：docs/craft-research/decision-brief-20260919.md——四项
  待裁决策（检索架构三方案/X 编码/时效规格形态/争议条目形态）逐项展开选项-机制-
  失效模式-切换阈值+默认建议（检索架构推荐选项 1：静态清单项库+运行时模型重排，
  向量 RAG 挂阈值待办）。建库仍暂缓，等裁决。

## 0.4.1 (2026-09-19)

- **工艺库全网调研收料完成**（F-11 勘误注已记）：16 查/~76 条 CR 论断，按分支入
  `docs/craft-research/`（00 总览+检索日志+源登记册+A/B/C/D/E/F/G/X 九文件）；
  每条带稳定 chunk ID（CR-<分支>-NN）+来源+检索日期+可信度分级+争议标——RAG-ready。
  **按用户指令暂缓建库**：条目繁复度评估（76→合并后 ~50-60 条）与三方案
  （静态精选/模型重排/向量 RAG）+四项待裁决问题见 00-overview §5。
- 深读缺口显式登记（web.dev/Hootsuite/Fstoppers 原文/ConradChavez——fetch 超时，
  本轮为搜索摘要级，待补读核数）；中文社区语料零覆盖待补。

## 0.4.0 (2026-09-19)

- **用户裁决 D11 落地**：默认通道=ps-mcp 的 COM 直连（S+F 不变）；**UXP 升格为预留
  第三通道**——01 分册 §6 从"定位说明"重写为"预留接口契约"（通道可换协议不变：
  铁律/四门/report 投影/五段式/实看义务通道无关；启用触发条件三条；启用路径自建
  面板＞临时借用 adb-mcp 只作验证；禁止静默切换与第三方面板常驻）。SKILL §0 与
  INSTALL 模式表同步标注"预留"。新指纹 #20/#21；新台账 **F-13**（预留接口未实现
  未实测，启用前置=research §6 #15 链路实测）。

## 0.3.0 (2026-09-19)

- **工艺库登记册**（[references/craft-library.md](references/craft-library.md)）：
  按路由分支动态分配的工艺条目容器——ID=CL-<分支码>-xx（与 00 分册 §0 的 A-H 对应，
  可记忆），路由判出主分支后读对应分支节；清单项=默认打法、候选=仅参考（禁止把候选
  当清单项执行）；写入走收工三问+实战出处强制，≥2 次实战升清单项，append-only。
  现挂空容器（专项调研 F-11 未启动），SKILL §3 与 00 分册 §0 已接线。
- 新指纹 #18/#19（候选不执行、无出处不登记）；新台账 **F-11**（工艺库大型专项调研
  未启动——扩充前置）与 **F-12**（启动期主窗口句柄漂移致模态误判——当日发现当日修，
  ps_connect.ps1 增加窗口稳定等待+同标题跳过）；F-2 加勘误注（警告重现频率 <100%）。
- **社区连接方案评估**（D10）：COM 族底层库 photoshop-python-api 本机实测通过（结果见
  docs/research §4.1）；两族 MCP 服务器均不引入包内（D6 立场维持），INSTALL 社区
  指路节更新成熟度评估与使用方式（给"用户自用 MCP 客户端"场景）。

## 0.2.0 (2026-09-19)

- **输入端路由层**（D9）：intake 新增第 0 步用途分支判定——8 分支（A 照片修图 /
  B 创意合成 / C 平面设计 / D UI 切图 / E 批处理 / F 绘画 / G AI 生成 / H 边界）+
  三信号判定表 + 路由后果映射（G1 强度/档位语义/快照密度/特别纪律）+ 不承接指路清单
  （3D→blender-mcp、剪辑→达芬奇、RAW 编目→LR、矢量→Illustrator）。依据=调研报告
  `docs/research-use-case-branches-20260919.md`（机器静态证据：内置脚本/预设目录/
  新建文档九类预设 [实测·静态]；3D 移除与 Substance Viewer 退役、动画时间轴仍在
  [网调 2026-09-19]）。00 分册 §0 / SKILL §3 第 0 步与 §5 档位分支注记落地。
- 新指纹 #15/#16/#17（分支判定第 0 步、不代画边界、不静默承接）；分册头部版本号
  移除（版本真源收敛到三处同步位）。
- 新台账 **F-9**（AI 生成/增强工具脚本可达性未实测，G 分支承接前置）/ **F-10**
  （文字图层与字体清单机读未实测，C 分支 G3 机验依赖）；研究底稿待实测清单增补
  #11-#14（AI 工具可达性/文字机读/画板 API/动作与变量数据集经 COM）。
- runner 自测探针动态化：P2 版本锚随 frontmatter 当前版本、P3 台账缺口锚随 CHANGELOG
  最新条目实际引用（原硬编码在升版/引用面变化后会静默失效——本次升版自测当场抓出，
  A6 再次实证）。

## 0.1.0 (2026-09-19)

- 初版（建包日）。双模式架构：**S=COM 直连**（Photoshop.Application + DoJavaScript，
  当日全链路实测：连接/建档/导出/回读/Quit）与 **F=JSX 文件总线兜底**（设计就位，
  全链路 F-3 待实测）。通道定案见 docs/decisions D3。
- 当日核心发现 **F-1（高）**：启动期可见模态框阻塞 COM 类厂激活（0x80080005）——
  连接 SOP 增加"启动模态扫描"步骤与三档处置（SKILL §0、references/01 §2、
  scripts/ps_connect.ps1 内置）；displayDialogs 与启动期模态分层澄清（D1）。
- 闸门 **F-2（中）**：C: 暂存盘 22GB 低于推荐值，启动警告预计重现——待用户决策，
  agent 侧兜底就位。
- 待实测登记：**F-3** F 模式全链路 / **F-4** COM 冷启动 / **F-5** 大串上限与
  DoJavaScriptFile / **F-6** executeMenuCommand 中文 UI / **F-7** suspendHistory 与
  历史栈 / **F-8** 多实例行为（清单见 docs/research §6）。
- 生态修订（D2）：官方 "Adobe for Creativity" 连接器（2026-04-28）为云端路线，不驱动
  本机桌面——本 skill 定位=本机桌面脚本级控制，互补不冲突；CEP 不碰、UXP 不启用。
- 治理层首日就位（D7）：14 指纹（grep 唯一验证后入表）+ 四查 runner（含 ④a/④b）+
  --self-test 反向实测（P1/P2/P3）；工艺层留白（03 册指纹 #13）。runner 首跑自噬抓出
  ④a 登记判定缺陷（全文扫描把台账交叉引用误判为已登记）——改为台账首列作用域，A6 实证。
